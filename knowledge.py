KNOWLEDGE = """
【概念】RAG（检索增强生成）的原理：大模型的知识截止于训练日期，且有时会"编造"不存在的事实。RAG 的做法是：用户提问时，先从外部知识库检索出最相关的几段资料，然后把资料和问题一起塞给模型，让模型"只根据资料回答"。这样做的好处是：回答有据可查、能处理最新信息、减少幻觉。RAG 的核心瓶颈在检索质量——检索不准则回答必然不准。
【实操】Python 基础该学到什么程度：转型 AI 应用开发不需要精通 Python，但要能看懂和修改别人代码。必须掌握的：数据类型（list/dict/set）、函数定义、文件读写、异常处理、pip 和虚拟环境。可以跳过或用到再学的：装饰器、元类、异步编程、C 扩展。关键判断标准：给你一段 50 行的 Python 脚本，你能不能说出它在干什么、能不能改几个地方让它做不同的事。
【概念】Embedding 和向量检索：Embedding（嵌入）就是把一段文字变成一串数字（向量）。这段数字不是随机的——语义相近的文字，向量的方向也相近。比如"程序员"和"开发者"虽然用词不同，但它们的向量夹角很小（余弦相似度高）。向量检索就是：把问题和知识片段都转成向量，然后算问题向量和每个片段向量的相似度，取最相似的几个返回。这和传统的关键词搜索完全不同——它能理解"同义不同词"。
【实操】大模型 API 怎么选：国内可用的主流选择——DeepSeek（便宜、中文好、开源模型可自己部署）、智谱 GLM（中文理解强、有免费额度、embedding 也好用）、通义千问（阿里生态、适合企业场景）。建议初学者先用 DeepSeek 跑通流程（便宜），然后再根据项目需求换。注意：所有国内 API 都要通过 base_url 指向不同的地址，但都兼容 OpenAI SDK 格式，改一行代码就能切换。
【概念】Prompt Engineering 的核心思路：写 prompt 不是"祈祷模型能懂"，而是"给模型明确的约束"。好的 prompt = 角色设定 + 任务描述 + 输入输出格式 + 关键规则 + 反面例子。比如你让模型生成 JSON 路线时，不仅要说要什么字段，还要告诉它"禁止用英文 key"、"阶段数必须在 3-6 之间"——把你能想到的失败模式提前堵死。
【实操】RAG 工程落地的三个细节：1）知识库切分（chunking）：太长的段落检索精度低，太短又丢失上下文，一般 100-500 字一段，具体按语义边界切（按句号/换行）。2）检索 Top-K 的选择：K 太小可能漏掉关键信息，K 太大塞进 prompt 浪费 token 且可能引入噪音，一般 2-5。3）检索后验证：检索回来的片段真的和问题相关吗？可以加一层简单的规则校验（比如必须有至少一个关键词命中），或者用 LLM 做二次判断。
【概念】AI Agent 到底是什么：Agent ≠ 调用 API 回答问题。Agent 的核心是"让模型能做事"——它能调用工具（搜索、计算、读写文件）、能分步骤规划、能根据结果调整下一步。简单说：普通对话是"问什么答什么"，Agent 是"给目标，它自己想办法完成"。工程上通常用 ReAct 模式（思考→行动→观察→再思考），把模型的推理能力和外部工具的执行能力结合起来。
【实操】Agent 开发的真实门槛：不是调个 API 就是 Agent。真正的挑战在于：1）工具定义要精确（参数类型、出错了怎么办）；2）多步推理容易跑偏（一步走错后面全歪）；3）输出格式不可控（Agent 中间步骤的输出下游程序要能解析）。建议先用 LangChain 或直接用 OpenAI function calling 做最小原型，理解 Agent 循环后再决定要不要自研框架。
【实操】转型 AI 应用开发的学习路线建议：第一阶段（1-2周）Python 速通 + 调通第一个 API；第二阶段（2-4周）理解 Prompt + 做一个完整的 API 调用项目（如路线生成工具）；第三阶段（4-6周）加上 RAG，理解检索和生成的配合；第四阶段（6-8周）做 Agent 或完整作品集项目。每个阶段都要有"可展示的输出"——一段代码、一个运行中的接口、一篇总结。
【概念】模型微调 vs RAG 的选择：微调是让模型"学新知识"，RAG 是给模型"查资料"。选哪个看场景：如果你有大量高质量的标注数据、需要模型掌握特定风格或格式，用微调；如果你需要模型回答关于最新文档/内部知识的问题、且数据经常更新，用 RAG。实际项目中 RAG 更常见——成本低、更新快、可解释性强。
【实操】AI 转型面试作品集怎么做：企业看三个东西——1）你有没有真的把东西做出来（跑得通的代码 > 花哨的 PPT）；2）你遇到了什么问题、怎么解决的（展示排查问题的能力）；3）你对自己的项目有没有反思（"这里我用了假 embedding，后来发现不行，换成了真的，效果差异是 X"——这种叙事比"我用了最先进的技术"有说服力十倍）。建议做成 GitHub 仓库 + README + 录屏演示。
【实操】新手最容易踩的三个坑：1）跳过基础直接撸复杂项目——连变量作用域都不懂就去抄 Agent 代码，必然卡死。先把最小版跑通（10行代码能证明概念），再往上加复杂度。2）把 LLM 当数据库用——模型会编造、会遗忘，不要靠它"记住"关键数据，数据要存自己的数据库。3）过早优化——知识库才 6 条就纠结用哪个向量数据库，先把流程跑通，瓶颈出现了再针对性解决。
【概念】RAG 的检索质量评估：怎么判断检索做得好不好？最简单的指标是"命中率"——你准备几个问题，每个问题你能人工判断哪些知识片段是相关的，看检索结果里有多少命中了。进阶可以用 MRR（平均倒数排名）——正确答案在检索结果里排第几位，排名越靠前越好。这是面试时能讲的"量化评估"。
【实操】Windows 环境下做 AI 开发的注意点：1）Python 安装建议用官方安装包而非 Microsoft Store 版；2）PowerShell 发中文请求容易乱码，发送端必须用 UTF-8 编码并指定 charset=utf-8；3）很多教程假设 Mac/Linux，遇到 pip 报错大概率是 C++ 编译工具链缺了——先搜"Windows 预编译 wheel"；4）代理设置可能干扰 API 调用，代码里加 trust_env=False 是个好习惯。
"""